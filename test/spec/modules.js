describe("Modules module", function() {
    var flag;

    // it("load()", function() {
    //     runs(function() {

    //         BMaps.modules.load('directions', function(name, module) {
    //             if(name === 'directions' && module.requested && module.loaded) flag = true;
    //         });

    //         waitsFor(function() {
    //             return flag;
    //         }, "Should load the requested module.", 1000);

    //     });
    // });
});